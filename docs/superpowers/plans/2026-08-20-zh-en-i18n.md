# 中英双语国际化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让数字孪生简历展馆、降级页和 PDF/Word 在中英之间完整切换，英文姓名为 Daopin Fu。

**Architecture:** `i18next` + `react-i18next` 只管理界面与 3D 提示文案；履历事实继续用平行 JSON（`resume-data.zh.json` / `resume-data.en.json`）供网页和 `generate_resume.py` 共用。语言解析顺序为 `?lang=` → `localStorage.i18nextLng` → `zh`。R3F 子树禁止 `useTranslation()`，由 `App` 传入 `locale`。

**Tech Stack:** React 19、TypeScript 5.9、Vite 7、Vitest 4、i18next、react-i18next、ReportLab/python-docx

## Global Constraints

- 合法 locale 仅为 `zh` 与 `en`；`zh-CN`/`zh-Hans` → `zh`，`en-US`/`en-GB` → `en`，其他值忽略。
- 不读取 `navigator.language`，不安装 `i18next-browser-languagedetector`，不用 `/en` 路径。
- 英文名必须是 `Daopin Fu`，职位 `Senior Java Engineer`。
- 技术专有名词（Spring Cloud、PostGIS、AgentScope、NL2SQL、Modbus、OPC）不翻译。
- 不卸载 WebGL Canvas，不因切语言递增 `sceneKey`。
- 不改展馆视觉、相机、模型几何、入场镜头，不为英文重排 PDF 页数。
- 中英 UI 词典 key 集合必须相等，叶子值不得为空。
- 两份履历 JSON 的 `projects[].id`、`topics[].id`、`pageSpan`、数组长度必须一致。
- 工作目录是 Git 仓库，每个 Task 的 Commit 步骤必须真正提交。

## File map

| Path | Responsibility |
|---|---|
| `web/src/i18n/locale.ts` | 纯函数：规范化、解析、同步 URL |
| `web/src/i18n/index.ts` | i18next 初始化、`changeAppLanguage`、`useLocale` |
| `web/src/i18n/locales/zh/ui.json` | 中文界面与 3D 提示 |
| `web/src/i18n/locales/en/ui.json` | 英文界面与 3D 提示 |
| `web/src/i18n/LanguageSwitcher.tsx` | `中 \| EN` 开关 |
| `web/src/data/resume-data.zh.json` | 现网履历改名 |
| `web/src/data/resume-data.en.json` | 英文履历 |
| `web/src/data/resume-data.ts` | 类型 + `getResumeData(locale)` |
| `web/src/data/model-representations.ts` | 按 locale 导出模型说明 |
| `web/src/data/engineering-console.ts` | 按 locale 导出终端文案 |
| `web/src/scene/scene-layout.ts` | 删除 `label`，只留几何 |
| `tools/generate_resume.py` | `--lang zh\|en\|all` + `CHROME` 字典 |

---

### Task 1: Locale 解析纯函数

**Files:**
- Create: `web/src/i18n/locale.ts`
- Create: `web/src/i18n/locale.test.ts`

**Interfaces:**
- Produces: `export type Locale = 'zh' | 'en'`
- Produces: `normalizeLocale(value: string | null | undefined): Locale | null`
- Produces: `resolveLocale(search: string, stored: string | null): Locale`
- Produces: `syncUrl(locale: Locale, search: string): string`

- [ ] **Step 1: 写失败测试**

```ts
import { describe, expect, it } from 'vitest';

import { normalizeLocale, resolveLocale, syncUrl } from './locale';

describe('locale resolution', () => {
  it('normalizes language tags to zh or en', () => {
    expect(normalizeLocale('zh')).toBe('zh');
    expect(normalizeLocale('zh-CN')).toBe('zh');
    expect(normalizeLocale('zh-Hans')).toBe('zh');
    expect(normalizeLocale('en')).toBe('en');
    expect(normalizeLocale('en-US')).toBe('en');
    expect(normalizeLocale('en-GB')).toBe('en');
    expect(normalizeLocale('fr')).toBeNull();
    expect(normalizeLocale('')).toBeNull();
    expect(normalizeLocale(null)).toBeNull();
  });

  it('prefers URL lang over localStorage then defaults to zh', () => {
    expect(resolveLocale('?lang=en', 'zh')).toBe('en');
    expect(resolveLocale('?mode=fallback&lang=en', null)).toBe('en');
    expect(resolveLocale('?lang=zh-CN', 'en')).toBe('zh');
    expect(resolveLocale('?lang=fr', 'en')).toBe('en');
    expect(resolveLocale('?mode=fallback', 'en-US')).toBe('en');
    expect(resolveLocale('', null)).toBe('zh');
    expect(resolveLocale('?lang=nope', null)).toBe('zh');
  });

  it('writes en to the query and strips lang for zh while keeping other params', () => {
    expect(syncUrl('en', '?mode=fallback')).toBe('?mode=fallback&lang=en');
    expect(syncUrl('en', '')).toBe('?lang=en');
    expect(syncUrl('zh', '?lang=en&mode=fallback')).toBe('?mode=fallback');
    expect(syncUrl('zh', '?lang=en')).toBe('');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd web && npm test -- --run src/i18n/locale.test.ts`

Expected: FAIL，无法解析 `./locale`。

- [ ] **Step 3: 实现最小 locale 模块**

```ts
export type Locale = 'zh' | 'en';

export function normalizeLocale(value: string | null | undefined): Locale | null {
  if (!value) return null;
  const base = value.trim().toLowerCase().split('-')[0];
  if (base === 'zh' || base === 'en') return base;
  return null;
}

export function resolveLocale(search: string, stored: string | null): Locale {
  const query = search.startsWith('?') ? search.slice(1) : search;
  const fromUrl = normalizeLocale(new URLSearchParams(query).get('lang'));
  if (fromUrl) return fromUrl;
  return normalizeLocale(stored) ?? 'zh';
}

export function syncUrl(locale: Locale, search: string): string {
  const query = search.startsWith('?') ? search.slice(1) : search;
  const params = new URLSearchParams(query);
  if (locale === 'en') params.set('lang', 'en');
  else params.delete('lang');
  const next = params.toString();
  return next ? `?${next}` : '';
}
```

- [ ] **Step 4: 再跑测试确认通过**

Run: `cd web && npm test -- --run src/i18n/locale.test.ts`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/src/i18n/locale.ts web/src/i18n/locale.test.ts
git commit -m "feat: add locale resolution helpers"
```

---

### Task 2: i18next 与中英 UI 词典

**Files:**
- Create: `web/src/i18n/locales/zh/ui.json`
- Create: `web/src/i18n/locales/en/ui.json`
- Create: `web/src/i18n/index.ts`
- Create: `web/src/i18n/ui-catalog.test.ts`
- Modify: `web/package.json`（增加 `i18next`、`react-i18next`）
- Modify: `web/src/main.tsx`

**Interfaces:**
- Consumes: `resolveLocale`, `syncUrl`, `Locale`
- Produces: `i18n` 单例、`currentLocale()`, `useLocale()`, `changeAppLanguage(next: Locale): Promise<void>`

- [ ] **Step 1: 安装依赖**

Run: `cd web && npm install i18next react-i18next`

不要安装 `i18next-browser-languagedetector`。

- [ ] **Step 2: 写词典 key 对齐失败测试**

```ts
import { describe, expect, it } from 'vitest';

import en from './locales/en/ui.json';
import zh from './locales/zh/ui.json';

function leafPaths(value: unknown, prefix = ''): string[] {
  if (typeof value === 'string') return [prefix];
  if (value && typeof value === 'object') {
    return Object.entries(value as Record<string, unknown>).flatMap(
      ([key, child]) => leafPaths(child, prefix ? `${prefix}.${key}` : key),
    );
  }
  return [];
}

describe('ui catalogs', () => {
  it('keeps zh and en keys aligned with non-empty leaves', () => {
    const zhPaths = leafPaths(zh).sort();
    const enPaths = leafPaths(en).sort();
    expect(enPaths).toEqual(zhPaths);
    for (const path of zhPaths) {
      const zhValue = path.split('.').reduce<unknown>((acc, key) => (acc as Record<string, unknown>)[key], zh);
      const enValue = path.split('.').reduce<unknown>((acc, key) => (acc as Record<string, unknown>)[key], en);
      expect(zhValue, path).toEqual(expect.any(String));
      expect(enValue, path).toEqual(expect.any(String));
      expect(String(zhValue).length, path).toBeGreaterThan(0);
      expect(String(enValue).length, path).toBeGreaterThan(0);
    }
  });
});
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd web && npm test -- --run src/i18n/ui-catalog.test.ts`

Expected: FAIL，找不到 json。

- [ ] **Step 4: 写入完整中英词典**

`web/src/i18n/locales/zh/ui.json`：

```json
{
  "meta": {
    "title": "付道品 | 高级 Java 开发工程师",
    "description": "付道品，高级 Java 开发工程师，智慧水务、Java 微服务、AIoT/GIS 与 Agent 工程化项目经历。"
  },
  "boot": { "initTitle": "初始化展馆资源..." },
  "language": { "zh": "中", "en": "EN", "switchToEn": "切换到英文", "switchToZh": "切换到中文" },
  "resume": {
    "pdfHref": "/resume/付道品-高级Java开发工程师.pdf"
  },
  "hud": {
    "skipToIndex": "跳到项目索引",
    "identityTitle": "点击查看核心能力与工作经历档案",
    "metricAria": "项目规模",
    "waterStations": "国内外水站",
    "skipIntro": "跳过动画",
    "coreResume": "核心履历",
    "coreResumeTitle": "查看核心能力矩阵与工作经历档案",
    "home": "返回中央馆",
    "zoomAria": "视野缩放控制",
    "zoomIn": "放大视野",
    "zoomInTitle": "放大视野 (快捷键: +)",
    "zoomOut": "缩小视野",
    "zoomOutTitle": "缩小视野 (快捷键: -)",
    "zoomReset": "复位视野",
    "zoomResetTitle": "复位视野 (快捷键: R / ESC)",
    "statusActive": "左键 360° 空间旋转 · 滚轮或 [+/-] 缩放视野 · 点击其他展位切换",
    "statusIdle": "WASD 漫游 · 滚轮或 [+/-] 缩放视野 · 点击展位对焦",
    "index": "展馆索引",
    "indexAria": "项目索引"
  },
  "loading": {
    "subIdle": "/// DISTRIBUTED SYSTEM INITIALIZATION",
    "subReady": "/// SYSTEM ONLINE · ALL CLUSTERS SYNCHRONIZED",
    "titleIdle": "分布式系统架构 · 数字孪生展馆",
    "titleReady": "展馆就绪 · 进入场景",
    "logs": {
      "engine": "初始化 WebGL 2.0 PBR 物理光照与高动态渲染管线",
      "topology": "加载 100,000+ 高并发工业测点分布式拓扑网络",
      "mesh": "构建微服务网格、多租户计算集群与分布式事务中枢",
      "optics": "编译深钛金反射着色器、等离子双螺旋与高能激光光效",
      "neural": "分布式控制中枢在线 · 准备就绪，进入 3D 沉浸式数字展馆"
    }
  },
  "mobileNotice": {
    "title": "建议使用电脑端浏览，以获得完整 3D 展馆体验",
    "body": "手机端继续后将呈现内容完整的纵向简历，包括项目背景、工程链路、职责边界与落地结果。",
    "continue": "继续查看简历",
    "close": "关闭提示"
  },
  "mobile": {
    "downloadPdf": "下载 PDF",
    "call": "拨打电话 {{phone}}",
    "mail": "发送邮件至 {{email}}",
    "strengths": "核心能力矩阵",
    "experience": "工作经历",
    "businessContext": "业务场景",
    "painPoints": "现有痛点",
    "buildGoals": "建设目标",
    "module": "模块",
    "flow": "业务链路",
    "boundary": "工程边界",
    "outcome": "落地结果",
    "stackIndex": "技术域索引"
  },
  "panel": {
    "expand": "展开案例详情",
    "expandTitle": "展开案例与架构详情",
    "collapse": "收起抽屉",
    "collapseTitle": "收起抽屉，全屏沉浸观察 3D 模型",
    "collapsePanel": "收起面板",
    "collapsePanelTitle": "收起面板 (全屏观察 3D 模型)",
    "downloadPdf": "下载 PDF 简历",
    "close": "关闭",
    "closeExhibit": "关闭展厅内容",
    "modelRepresents": "3D 模型代表实际系统架构",
    "modelMetaphor": "3D 视觉构件隐喻与架构映射",
    "role": "本人角色",
    "background": "项目背景",
    "business": "01 / 业务场景",
    "pain": "02 / 现有痛点",
    "goals": "03 / 建设目标",
    "flow": "业务链路",
    "boundary": "工程边界",
    "implementation": "核心实现",
    "challenges": "技术难点",
    "stack": "技术矩阵"
  },
  "overview": {
    "titleSuffix": "核心简历档案",
    "download": "下载 PDF",
    "downloadTitle": "下载最新 PDF 简历",
    "close": "关闭档案面板",
    "position": "职业定位",
    "strengths": "A1 · 核心能力矩阵",
    "experience": "A2 · 工作经历",
    "stack": "技术域索引",
    "museum": "3D 展馆"
  },
  "console": {
    "title": "工程终端",
    "stamp": "履历证据 / 静态演示",
    "channelsAria": "工程终端频道"
  },
  "exhibits": {
    "litree-overview": "微服务与数据底座",
    "litree-aiot": "AIoT 与空间拓扑",
    "litree-agent": "水务智能体",
    "oa-hr": "立升 OA / HR",
    "welink-search": "WeLink 统一搜索",
    "welink-data": "WeLink 双路数据湖",
    "senge-gateway": "森格实时通信网关",
    "senge-platform": "森格 0-1 平台架构"
  },
  "scene": {
    "billboards": {
      "west1": "亿级高并发实时调度架构",
      "west2": "国内外智慧水站空间拓扑",
      "west3": "全链路分布式高可用治理",
      "east1": "生产级企业智能体编排中台",
      "east2": "双路实时/离线数据湖治理",
      "east3": "华为云 WeLink 千万级搜索"
    },
    "hologram": {
      "coreValues": "CORE VALUES: 高并发 · AIoT 空间计算 · 双路数据湖 · 智能体微服务中台",
      "metricWater": "国内外水站高并发 AIoT 接入 · 毫秒调度",
      "metricSearch": "华为云千亿级文档搜索 · 动态个性化打分",
      "metricGateway": "Netty/WebSocket 长连接网关 · 告警风暴治理",
      "metricAgent": "LangChain/Spring AI 智能体编排 · 沙箱执行"
    },
    "tag": {
      "ready": "● 3D 孪生就绪 · 待聚焦",
      "entityPrefix": "代表业务: ",
      "click": "▶ 点击对焦观察 3D 模型架构",
      "inspecting": "● 正在对焦观察中",
      "inspectHint": "⚡ 鼠标拖拽 360° 旋转 · 滚轮缩放 · 右侧查看案例详情"
    }
  }
}
```

`web/src/i18n/locales/en/ui.json`：同一 key 树，值为：

```json
{
  "meta": {
    "title": "Daopin Fu | Senior Java Engineer",
    "description": "Daopin Fu, Senior Java Engineer. Smart water, Java microservices, AIoT/GIS, and Agent engineering."
  },
  "boot": { "initTitle": "Initializing museum assets..." },
  "language": { "zh": "中", "en": "EN", "switchToEn": "Switch to English", "switchToZh": "Switch to Chinese" },
  "resume": {
    "pdfHref": "/resume/Daopin-Fu-Senior-Java-Engineer.pdf"
  },
  "hud": {
    "skipToIndex": "Skip to project index",
    "identityTitle": "Open core skills and work history",
    "metricAria": "Project scale",
    "waterStations": "Water stations worldwide",
    "skipIntro": "Skip intro",
    "coreResume": "Resume",
    "coreResumeTitle": "View competency matrix and work history",
    "home": "Return to plaza",
    "zoomAria": "Viewport zoom",
    "zoomIn": "Zoom in",
    "zoomInTitle": "Zoom in (shortcut: +)",
    "zoomOut": "Zoom out",
    "zoomOutTitle": "Zoom out (shortcut: -)",
    "zoomReset": "Reset view",
    "zoomResetTitle": "Reset view (shortcut: R / ESC)",
    "statusActive": "Left-drag to orbit 360° · wheel or [+/-] to zoom · click another exhibit to switch",
    "statusIdle": "WASD to roam · wheel or [+/-] to zoom · click an exhibit to focus",
    "index": "Museum index",
    "indexAria": "Project index"
  },
  "loading": {
    "subIdle": "/// DISTRIBUTED SYSTEM INITIALIZATION",
    "subReady": "/// SYSTEM ONLINE · ALL CLUSTERS SYNCHRONIZED",
    "titleIdle": "Distributed systems · digital twin museum",
    "titleReady": "Museum ready · entering scene",
    "logs": {
      "engine": "Initializing WebGL 2.0 PBR lighting and HDR pipeline",
      "topology": "Loading 100,000+ industrial telemetry topology",
      "mesh": "Building microservice mesh, multi-tenant clusters, and distributed transaction core",
      "optics": "Compiling titanium shaders, plasma helices, and laser effects",
      "neural": "Control plane online · entering the 3D museum"
    }
  },
  "mobileNotice": {
    "title": "Use a desktop browser for the full 3D museum",
    "body": "Continue on mobile to read the complete vertical resume, including project context, engineering flow, ownership boundaries, and outcomes.",
    "continue": "Continue to resume",
    "close": "Dismiss notice"
  },
  "mobile": {
    "downloadPdf": "Download PDF",
    "call": "Call {{phone}}",
    "mail": "Email {{email}}",
    "strengths": "Core competency matrix",
    "experience": "Work experience",
    "businessContext": "Business context",
    "painPoints": "Pain points",
    "buildGoals": "Build goals",
    "module": "Module",
    "flow": "Flow",
    "boundary": "Engineering boundary",
    "outcome": "Outcome",
    "stackIndex": "Technology index"
  },
  "panel": {
    "expand": "Expand case details",
    "expandTitle": "Expand case and architecture details",
    "collapse": "Collapse drawer",
    "collapseTitle": "Collapse drawer for a full-screen 3D view",
    "collapsePanel": "Collapse panel",
    "collapsePanelTitle": "Collapse panel (full-screen 3D view)",
    "downloadPdf": "Download PDF resume",
    "close": "Close",
    "closeExhibit": "Close exhibit details",
    "modelRepresents": "The 3D model represents the live system",
    "modelMetaphor": "Visual components mapped to architecture",
    "role": "Role",
    "background": "Background",
    "business": "01 / Context",
    "pain": "02 / Pain points",
    "goals": "03 / Goals",
    "flow": "Flow",
    "boundary": "Engineering boundary",
    "implementation": "Implementation",
    "challenges": "Challenges",
    "stack": "Stack"
  },
  "overview": {
    "titleSuffix": "Career dossier",
    "download": "Download PDF",
    "downloadTitle": "Download the latest PDF resume",
    "close": "Close dossier",
    "position": "Positioning",
    "strengths": "A1 · Core competency matrix",
    "experience": "A2 · Work experience",
    "stack": "Technology index",
    "museum": "3D museum"
  },
  "console": {
    "title": "Engineering console",
    "stamp": "Career evidence / static demo",
    "channelsAria": "Engineering console channels"
  },
  "exhibits": {
    "litree-overview": "Microservices & Data Foundation",
    "litree-aiot": "AIoT & Spatial Topology",
    "litree-agent": "Water Data Agent",
    "oa-hr": "Litree OA / HR",
    "welink-search": "WeLink Unified Search",
    "welink-data": "WeLink Dual-Path Data Lake",
    "senge-gateway": "Senge Realtime Gateway",
    "senge-platform": "Senge 0-to-1 Platform"
  },
  "scene": {
    "billboards": {
      "west1": "Billion-scale realtime scheduling fabric",
      "west2": "Spatial topology for water stations worldwide",
      "west3": "End-to-end distributed availability",
      "east1": "Production agent orchestration platform",
      "east2": "Dual-path realtime / offline lake governance",
      "east3": "Huawei Cloud WeLink 10M+ search"
    },
    "hologram": {
      "coreValues": "CORE VALUES: High concurrency · AIoT spatial compute · Dual-path lake · Agent microservices",
      "metricWater": "High-concurrency AIoT ingest for water stations worldwide",
      "metricSearch": "Huawei Cloud search with dynamic personalized scoring",
      "metricGateway": "Netty/WebSocket long-lived gateway · alarm-storm control",
      "metricAgent": "LangChain/Spring AI orchestration · sandboxed execution"
    },
    "tag": {
      "ready": "● 3D twin ready · click to focus",
      "entityPrefix": "Represents: ",
      "click": "▶ Click to inspect the 3D architecture",
      "inspecting": "● Inspecting",
      "inspectHint": "⚡ Drag to orbit 360° · scroll to zoom · details on the right"
    }
  }
}
```

- [ ] **Step 5: 实现 i18n 入口**

`web/src/i18n/index.ts`：

```ts
import i18n from 'i18next';
import { useTranslation } from 'react-i18next';

import { type Locale, normalizeLocale, resolveLocale, syncUrl } from './locale';
import enUi from './locales/en/ui.json';
import zhUi from './locales/zh/ui.json';

function readStoredLocale(): string | null {
  try {
    return window.localStorage.getItem('i18nextLng');
  } catch {
    return null;
  }
}

export function currentLocale(resolved = i18n.resolvedLanguage): Locale {
  return normalizeLocale(resolved) ?? 'zh';
}

export function useLocale(): Locale {
  const { i18n: instance } = useTranslation();
  return currentLocale(instance.resolvedLanguage);
}

export function applyDocumentMeta(locale: Locale) {
  const t = i18n.getFixedT(locale);
  document.documentElement.lang = locale === 'en' ? 'en' : 'zh-CN';
  document.title = t('meta.title');
  const description = document.querySelector('meta[name="description"]');
  if (description) description.setAttribute('content', t('meta.description'));
}

export async function changeAppLanguage(next: Locale) {
  await i18n.changeLanguage(next);
  const url = new URL(window.location.href);
  url.search = syncUrl(next, url.search);
  window.history.replaceState(null, '', `${url.pathname}${url.search}${url.hash}`);
  applyDocumentMeta(next);
}

const initialLng = typeof window === 'undefined'
  ? 'zh'
  : resolveLocale(window.location.search, readStoredLocale());

void i18n.init({
  lng: initialLng,
  fallbackLng: 'zh',
  supportedLngs: ['zh', 'en'],
  ns: ['ui'],
  defaultNS: 'ui',
  resources: {
    zh: { ui: zhUi },
    en: { ui: enUi },
  },
  interpolation: { escapeValue: false },
});

if (typeof window !== 'undefined') {
  applyDocumentMeta(currentLocale());
  i18n.on('languageChanged', (lng) => {
    applyDocumentMeta(currentLocale(lng));
  });
}

export { i18n };
```

`web/src/main.tsx` 在渲染 App 前 `import './i18n'`：

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { I18nextProvider } from 'react-i18next';

import App from './App';
import { i18n } from './i18n';
import './styles.css';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <I18nextProvider i18n={i18n}>
      <App />
    </I18nextProvider>
  </StrictMode>,
);
```

- [ ] **Step 6: 跑词典测试确认通过**

Run: `cd web && npm test -- --run src/i18n/ui-catalog.test.ts src/i18n/locale.test.ts`

Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add web/package.json web/package-lock.json web/src/i18n web/src/main.tsx
git commit -m "feat: initialize i18next with zh/en UI catalogs"
```

---

### Task 3: LanguageSwitcher

**Files:**
- Create: `web/src/i18n/LanguageSwitcher.tsx`
- Modify: `web/src/styles/museum.css`（在 `.museum-hud__commands` 后追加开关样式）

**Interfaces:**
- Consumes: `changeAppLanguage`, `useLocale`
- Produces: `<LanguageSwitcher />`

- [ ] **Step 1: 实现开关组件**

没有 jsdom，不写 React 渲染测试。切换逻辑已由 Task 1 的 `syncUrl` 覆盖。

```tsx
import { useTranslation } from 'react-i18next';

import { changeAppLanguage, useLocale } from './index';

export function LanguageSwitcher() {
  const locale = useLocale();
  const { t } = useTranslation();

  return (
    <div className="language-switcher" role="group" aria-label={t('language.switchToEn')}>
      <button
        type="button"
        className={locale === 'zh' ? 'text-command text-command--cyan' : 'text-command'}
        aria-pressed={locale === 'zh'}
        aria-label={t('language.switchToZh')}
        onClick={() => void changeAppLanguage('zh')}
      >
        {t('language.zh')}
      </button>
      <button
        type="button"
        className={locale === 'en' ? 'text-command text-command--cyan' : 'text-command'}
        aria-pressed={locale === 'en'}
        aria-label={t('language.switchToEn')}
        onClick={() => void changeAppLanguage('en')}
      >
        {t('language.en')}
      </button>
    </div>
  );
}
```

在 `web/src/styles/museum.css` 的 `.museum-hud__commands` 规则后追加：

```css
.language-switcher {
  display: inline-flex;
  gap: 0;
}

.language-switcher .text-command {
  min-width: 44px;
  padding: 0 12px;
}

.language-switcher .text-command:first-child {
  border-top-right-radius: 0;
  border-bottom-right-radius: 0;
}

.language-switcher .text-command:last-child {
  border-top-left-radius: 0;
  border-bottom-left-radius: 0;
  margin-left: -1px;
}
```

- [ ] **Step 2: Commit**

```bash
git add web/src/i18n/LanguageSwitcher.tsx web/src/styles/museum.css
git commit -m "feat: add zh/en language switcher"
```

本 Task 尚未把开关挂到 HUD；Task 6 接入。组件本身可独立审查。

---

### Task 4: 履历 JSON 拆分与英文稿

**Files:**
- Rename: `web/src/data/resume-data.json` → `web/src/data/resume-data.zh.json`（内容不改）
- Create: `web/src/data/resume-data.en.json`
- Modify: `web/src/data/resume-data.ts`
- Modify: `web/src/data/resume-data.test.ts`

**Interfaces:**
- Produces: `getResumeData(locale: Locale): ResumeData`
- Produces: `getTopic(topicId: string, locale: Locale): Topic | undefined`
- Produces: `getProject(projectId: string, locale: Locale): Project | undefined`
- 删除默认导出常量 `resumeData`。所有调用方在 Task 6 改为 `getResumeData(locale)`。本 Task 先让测试通过；若 `tsc` 因旧 import 失败，可临时保留 `export const resumeData = getResumeData('zh')` 直到 Task 6 删掉。保留该兼容导出，Task 6 再删除。

- [ ] **Step 1: 扩展失败测试**

把现有 `resume-data.test.ts` 的 `resumeData` 全部改为 `getResumeData('zh')`。追加：

```ts
import { getResumeData } from './resume-data';

const zh = getResumeData('zh');
const en = getResumeData('en');

it('keeps English resume structurally aligned and uses Daopin Fu', () => {
  expect(en.profile.name).toBe('Daopin Fu');
  expect(en.profile.title).toBe('Senior Java Engineer');
  expect(en.experiences.map((item) => item.company)).toEqual([
    'Litree Water Purification Technology',
    'Adecco',
    'Senge Automation Technology',
  ]);
  expect(en.projects.map((project) => project.id)).toEqual(zh.projects.map((project) => project.id));
  expect(en.projects.map((project) => project.pageSpan)).toEqual(zh.projects.map((project) => project.pageSpan));
  expect(en.projects.map((project) => project.topics.map((topic) => topic.id)))
    .toEqual(zh.projects.map((project) => project.topics.map((topic) => topic.id)));
  expect(en.highlights).toHaveLength(zh.highlights.length);
  expect(en.strengths).toHaveLength(zh.strengths.length);
  expect(JSON.stringify(en)).toContain('AgentScope Java 2.0');
  expect(JSON.stringify(en)).toContain('10w+');
  expect(JSON.stringify(en)).toContain('NL2SQL');
  expect(JSON.stringify(en)).not.toContain('北京智能信通科技');
});
```

中文原有断言保持，只是数据源改成 `getResumeData('zh')`。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd web && npm test -- --run src/data/resume-data.test.ts`

Expected: FAIL，因为还没有 `getResumeData` / 英文 JSON。

- [ ] **Step 3: 改名中文 JSON 并实现加载器**

把 `resume-data.json` 原样改名为 `resume-data.zh.json`。

`web/src/data/resume-data.ts`：

```ts
import type { Locale } from '../i18n/locale';
import en from './resume-data.en.json';
import zh from './resume-data.zh.json';

export interface Topic {
  id: string;
  title: string;
  background: string;
  role: string;
  flow: string;
  engineeringBoundary: string;
  implementation: string[];
  challenges: string[];
  outcome: string;
  stack: string[];
}

export interface Project {
  id: string;
  name: string;
  company: string;
  period: string;
  pageSpan: number;
  summary: string;
  businessContext: string;
  painPoints: string[];
  buildGoals: string[];
  role: string;
  flow: string;
  engineeringBoundary: string;
  outcome: string;
  stack: string[];
  topics: Topic[];
}

export interface Experience {
  period: string;
  company: string;
  title: string;
  summary: string;
  achievements: string[];
}

export interface ResumeData {
  profile: {
    name: string;
    title: string;
    experience: string;
    phone: string;
    email: string;
    website?: string;
    summary: string;
  };
  highlights: string[];
  strengths: Array<{ title: string; evidence: string }>;
  experiences: Experience[];
  projects: Project[];
}

const catalogs: Record<Locale, ResumeData> = {
  zh: zh as ResumeData,
  en: en as ResumeData,
};

export function getResumeData(locale: Locale): ResumeData {
  return catalogs[locale];
}

export const resumeData = getResumeData('zh');

export function getTopic(topicId: string, locale: Locale = 'zh'): Topic | undefined {
  return getResumeData(locale).projects.flatMap((project) => project.topics).find((topic) => topic.id === topicId);
}

export function getProject(projectId: string, locale: Locale = 'zh'): Project | undefined {
  return getResumeData(locale).projects.find((project) => project.id === projectId);
}
```

- [ ] **Step 4: 写入完整英文履历 `web/src/data/resume-data.en.json`**

结构必须与中文 JSON 逐字段对齐。电话、邮箱、网站、stack 数组保持原文。正文如下（整文件写入，不要删 topic）：

```json
{
  "profile": {
    "name": "Daopin Fu",
    "title": "Senior Java Engineer",
    "experience": "~6 years of Java backend engineering",
    "phone": "17322443268",
    "email": "fudaopin@icloud.com",
    "website": "http://cv.bookfree.online/",
    "summary": "Java distributed microservices, industrial AIoT and spatial GIS, enterprise OA/HR platforms, reliable realtime/offline data engineering, and production Agent systems. Designs complex business backends, governs high-availability data, and delivers 0-to-1 systems end to end."
  },
  "highlights": [
    "Covers 10w+ water stations worldwide",
    "Java 21 and Spring Cloud microservices",
    "Industrial AIoT protocols (UsrCloud/MQTT/Modbus/OPC)",
    "PostGIS topology / pruned BFS valve isolation / DMA",
    "Kafka / Flink realtime streams with optimistic-lock versioning",
    "Hadoop / Spark offline lake and historical backfill",
    "Elasticsearch hybrid retrieval and affinity scoring",
    "AgentScope Java 2.0 runtime with sandbox isolation",
    "Spring AI Alibaba Graph / NL2SQL validation loop",
    "OA/HR attendance rule engine / performance state machine / SSO"
  ],
  "strengths": [
    {
      "title": "Java distributed microservices and multi-tenant architecture",
      "evidence": "Deep in Java 21 / Spring Boot 3 / Spring Cloud. Owns TransmittableThreadLocal tenant-context propagation, distributed locks, multi-level cache, and cross-service eventual consistency."
    },
    {
      "title": "Industrial AIoT ingest and thing-model governance",
      "evidence": "Integrated UsrCloud, MQTT, Modbus TCP/RTU, TCP/UDP meters, FanYi, Maituo, and Gukon OPC/HTTP. Delivered dual-token auth, rate limits, TCP framing, clock checks, and TSL thing-model normalization."
    },
    {
      "title": "PostGIS topology and DMA leakage algorithms",
      "evidence": "Uses PostGIS spatial indexes and directed adjacency lists. Designed a pruned BFS to compute minimum valve-isolation sets in milliseconds, plus MNF night-flow leakage analysis."
    },
    {
      "title": "Agent systems and AI Coding in production",
      "evidence": "Landed AgentScope Java 2.0 and Spring AI Alibaba StateGraph with persistent Checkpointer, HITL approval, NL2SQL AST checks, and Docker sandbox isolation."
    }
  ],
  "experiences": [
    {
      "period": "Oct 2023 – Present",
      "company": "Litree Water Purification Technology",
      "title": "Senior Java Engineer",
      "summary": "Built Litree Smart Water Cloud and the OA/HR platform in parallel: device ledger, multi-protocol ingest, GIS topology, and Agent engineering on the water side; attendance, performance, and master-data sync on the OA side.",
      "achievements": [
        "Owned the microservice foundation, device ledger, multi-protocol ingest, GIS/DMA, and data agent for Litree Smart Water Cloud covering 10w+ stations worldwide.",
        "Independently delivered the OA/HR attendance rule engine, performance state machine, and idempotent master-data sync, then linked org permissions with the water platform.",
        "Unified station master data, AIoT ingest, GIS topology, and Agent Q&A onto one microservice and permission model so monitoring and the business platform do not keep two account systems."
      ]
    },
    {
      "period": "Oct 2021 – Oct 2023",
      "company": "Adecco",
      "title": "Java / Big Data Engineer",
      "summary": "Worked on Huawei WeLink Xiaowei Search: unified people, groups, and chat retrieval, profile/affinity rules, and 10M-scale realtime/offline ingest pipelines.",
      "achievements": [
        "Owned unified retrieval across people, groups, and chats, plus pinyin correction, affinity scoring, and Elasticsearch hybrid-search tuning.",
        "Built Kafka/Flink realtime and Spark offline dual ingest, killed out-of-order overwrites with external-version optimistic locks, and served as QC/Committer for release reviews.",
        "Used offline Spark to backfill history and missing links so realtime streams and the lake share one search contract."
      ]
    },
    {
      "period": "Sep 2020 – Oct 2021",
      "company": "Senge Automation Technology",
      "title": "Java Engineer",
      "summary": "Built a smart-water monitoring platform from 0 to 1: field ingest, realtime fan-out, dashboards, alarm-storm control, and containerized delivery.",
      "achievements": [
        "Stood up auth, monitoring, reports, and archive from scratch, and connected field ingest to WebSocket dashboards.",
        "Shipped sliding-window alarm debounce, multi-channel dispatch, idempotent remote control, and Docker Compose multi-env delivery.",
        "Delivered water-quality/pressure reports, historical archive, and remote control on the same 0-to-1 stack so the plant is operable and replayable."
      ]
    }
  ],
  "projects": [
    {
      "id": "litree",
      "name": "Litree Smart Water Cloud",
      "company": "Litree Water Purification Technology",
      "period": "Oct 2023 – Present",
      "pageSpan": 2,
      "summary": "Unified smart-water platform for 10w+ stations worldwide, connecting station master data, AIoT devices, process monitoring, alarms, GIS networks, analytics, and an in-platform water data agent.",
      "businessContext": "Litree digitally manages 10w+ stations worldwide across project spaces, stations, gateways, IoT devices, process monitoring, alarms, GIS, and analytics. A station’s life cycle spans assets, devices, space, and realtime data.",
      "painPoints": [
        "Stations, project spaces, gateways, devices, and thing models live in multiple services; identifiers, life cycles, and data contracts are not naturally consistent.",
        "Global business needs one identity, tenant, permission, and isolation model while absorbing UsrCloud, MQTT, Modbus, TCP/UDP, OPC, and spatial relations.",
        "Monitoring, alarms, network analysis, and agent tools all depend on a trusted data path with explicit contracts, compensation, and ownership boundaries."
      ],
      "buildGoals": [
        "Connect water objects through unified auth, tenant context, data permissions, and ID mapping.",
        "Open the path from station onboarding and device ingest through monitoring, alarms, GIS/DMA, and analytics.",
        "Land controllable, recoverable, auditable Agent and AI Coding practice inside the Java stack."
      ],
      "role": "Owned the device ledger, industrial protocol gateway, GIS topology, and water data agent; joined microservice collaboration, cross-module integration, and production incident work.",
      "flow": "Project space & station → multi-gateway topology → IoT ledger → protocol thing-model → realtime monitor/alarm → GIS topology → water data agent",
      "engineeringBoundary": "Owns device ledger, multi-protocol ingest, GIS/DMA, and Agent engineering, covering UsrCloud, MQTT, Modbus TCP/RTU, TCP/UDP meters, FanYi, Maituo, and Gukon OPC/HTTP.",
      "outcome": "Closed the water backend loop from onboarding and ingest through monitoring, alarms, network analysis, and the data agent, supporting 10w+ stations.",
      "stack": ["Java 21", "Spring Boot 3", "Spring Cloud", "Nacos", "OpenFeign", "PostgreSQL/PostGIS", "Redis", "TDengine", "MQTT", "Modbus", "TCP/UDP", "OPC/HTTP", "AgentScope Java 2.0", "Spring AI Graph"],
      "topics": [
        {
          "id": "litree-overview",
          "title": "Microservice foundation and device ledger",
          "background": "Built a multi-tenant microservice foundation for 10w+ stations to keep multi-source ledgers, multi-gateway topology, and bulk validation consistent.",
          "role": "Owned tenant-context propagation, station-gateway-device topology, second-scale bulk ledger import, and cross-service consistency.",
          "flow": "Project space & station → multi-gateway topology → bulk ledger import → thing-model mapping → runtime stats",
          "engineeringBoundary": "Built the foundation on unified auth, end-to-end tenant-context propagation, and ID mapping, with row-level isolation and idempotent compensation.",
          "implementation": [
            "End-to-end multi-tenant isolation via TransmittableThreadLocal, Feign interceptors, and MyBatis plugins, with safe context cleanup.",
            "Caffeine + Redis cache plus distributed locks to stop stampede, penetration, and avalanches; hot config stays in local L1.",
            "Compare IoT status by station, drop empty or fully-offline sites, and unify station-gateway-device mapping.",
            "EasyExcel + JSR-303 row validation for 10k-scale ledgers; failed rows return with locations without aborting the batch.",
            "Idempotent compensation between local ledger writes and remote IoT registration so retries never duplicate devices.",
            "Generate thing-model attributes from templates and use Redisson watchdog locks to stop concurrent mock reports and tenant bleed.",
            "Reconcile station master data with IoT registration on a timer; exceptions enter a compensation queue instead of silent loss."
          ],
          "challenges": [
            "ThreadLocal is easy to drop or leak across pools and schedulers; wrap the life cycle and always clean up.",
            "One station may have many gateways, inactive gateways, and stale ledger rows; import failures must isolate by row, not roll back the batch.",
            "Cross-service registration can partially succeed; converge with business-key idempotency and compensation jobs.",
            "Cache and lock keys must include tenant, or bulk import and registration will cross-write."
          ],
          "outcome": "Opened the main path from onboarding, gateway binding, and device registration to monitoring stats, with reconcilable, compensable IoT registration.",
          "stack": ["Java 21", "Spring Boot 3", "Spring Cloud", "Nacos", "OpenFeign", "MyBatis", "PostgreSQL", "Redis", "Redisson", "Quartz", "EasyExcel"]
        },
        {
          "id": "litree-aiot",
          "title": "Industrial AIoT and GIS networks",
          "background": "Field protocols include UsrCloud, MQTT, Modbus, TCP/UDP meters, FanYi, Maituo, and Gukon OPC/HTTP. Thing models must unify before topology and DMA.",
          "role": "Owned UsrCloud/MQTT/Modbus/TCP/UDP/OPC ingest and thing models, plus GIS valve isolation and DMA.",
          "flow": "UsrCloud/MQTT/Modbus/TCP/UDP/OPC → device identity match → thing-model cleanup → PostGIS topology → pruned BFS → DMA leakage alert",
          "engineeringBoundary": "Owns full-path ingest and thing-model mapping for UsrCloud, MQTT, Modbus TCP/RTU, TCP/UDP meters, FanYi, Maituo, and Gukon OPC/HTTP.",
          "implementation": [
            "UsrCloud HTTP ingest: Redis dual-token refresh, token-bucket limits, and a bounded pool for paged collection, isolating single-device faults.",
            "MQTT ingest: product/tenant topic routing, last will, and reconnect; map properties/events into the unified thing model.",
            "Modbus TCP/RTU and TCP/UDP meters: framing, endianness, CRC, and register high/low mapping.",
            "FanYi, Maituo, and Gukon OPC/HTTP gateways share a Thing Component interface for config, periodic sync, and fault isolation.",
            "TSL thing-model normalization: clean points and units; time-window checks stop offline backfill from dirty overwrites.",
            "PostGIS GIST one-map plus pruned BFS minimum valve sets, with DMA trees and MNF leakage alerts.",
            "Quarantine unmatched reports so dirty points cannot pollute the thing model or DMA stats."
          ],
          "challenges": [
            "HTTP clouds, MQTT reports, Modbus registers, and TCP framing differ; the ingest layer must stay decoupled from the unified thing model.",
            "Bad device clocks or offline backfill can invert time; use windows and latest-state guards.",
            "Networks have loops and isolated subgraphs; traversals need cycle detection and pruning.",
            "The same physical device may report over different protocols; merge by thing-model identity."
          ],
          "outcome": "Brought multi-protocol devices onto one IoT path with a network one-map, valve isolation, and DMA leakage monitoring.",
          "stack": ["Java", "Spring Boot", "HTTP", "MQTT", "Modbus TCP/RTU", "TCP/UDP", "OPC/HTTP", "PostgreSQL/PostGIS", "Redis", "TDengine", "Thing model"]
        },
        {
          "id": "litree-agent",
          "title": "Water data agent and AI Coding",
          "background": "Landed a controllable, recoverable, auditable data agent inside the water microservices, and turned a real water project into an AI Coding workflow.",
          "role": "Drove AgentScope Java adaptation and Spring AI agents, including NL2SQL, HITL, sandbox execution, and AI Coding standards.",
          "flow": "User intent → StateGraph / schema prune → NL2SQL → HITL → container sandbox → report",
          "engineeringBoundary": "Agents always enforce permission checks, human approval, and sandbox isolation; models are never exposed as writable production APIs.",
          "implementation": [
            "Adapted AgentScope Java 2.0 into a HarnessAgent / ReActAgent / Toolkit / Memory / Session runtime.",
            "Spring AI Alibaba StateGraph orchestrates intent, schema prune, Planner, NL2SQL, Python analysis, and report nodes.",
            "Wrapped station, device, thing-model, alarm, ticket, GIS, and stats queries as Tool / MCP, clipped by tenant and data permission.",
            "Persistent Checkpointer supports HITL resume and SSE; AST checks plus Self-Correction constrain NL2SQL.",
            "Docker pool runs Python analysis against read-only sources with network/FS limits.",
            "AGENTS.md, Skills, and CI gates close the AI Coding loop from requirement through review and release.",
            "Tool calls record session, tenant, SQL, and approver; skip HITL and execution is refused."
          ],
          "challenges": [
            "Long-running agents retry, pause for humans, and drift; they need durable state and SSE session management.",
            "Models invent cartesian-product or write SQL; block with AST parsing and read-only sources.",
            "Tools can cross tenants; enforce session, tenant, and data permission at the Tool gate.",
            "Oversized schemas must be pruned by permission or Planner picks the wrong table or times out."
          ],
          "outcome": "Shipped an approvable, sandboxed, auditable water data agent and a durable AI Coding workflow.",
          "stack": ["AgentScope Java 2.0", "Spring AI Graph", "NL2SQL", "Docker Sandbox", "SSE", "AGENTS.md"]
        }
      ]
    },
    {
      "id": "oa",
      "name": "Litree OA / HR System",
      "company": "Litree Water Purification Technology",
      "period": "Oct 2023 – Present",
      "pageSpan": 1,
      "summary": "Internal OA/HR platform covering org/HR, attendance, performance, and SSO, syncing people, orgs, and station master data to the smart-water platform.",
      "businessContext": "OA/HR is independent of water monitoring. It owns hire/transfer/leave, flexible shifts, daily attendance close, multi-level performance, and SSO. Water users, orgs, and station permissions depend on this master data, but attendance/performance is not a water-module add-on.",
      "painPoints": [
        "Attendance rules include overnight shifts, flextime, and leave/overtime offsets; raw punch logs cannot produce reports.",
        "Performance needs plan config, metric rollout, multi-level weights, and version snapshots without losing state.",
        "Org changes must sync idempotently into the water permission domain to avoid duplicate accounts, delayed access, or tenant bleed."
      ],
      "buildGoals": [
        "Deliver the attendance rule engine, performance state machine, and HR life-cycle loop.",
        "Deduplicate in memory by business key and batch-upsert so cross-system master data converges.",
        "Unify auth and org permissions so water accounts take effect as soon as HR changes."
      ],
      "role": "Owned the attendance rule engine, performance state machine, org master-data sync, and SSO integration.",
      "flow": "Org/HR → shift attendance → document offset & daily close → performance review → master-data sync → water permissions",
      "engineeringBoundary": "Focuses on OA/HR rules and master-data sync; water monitoring, protocol ingest, and Agent capabilities belong to Smart Water Cloud.",
      "outcome": "Delivered a closed OA/HR platform and kept people, orgs, and station permissions aligned with the water platform.",
      "stack": ["Spring Boot", "MyBatis-Plus", "PostgreSQL", "Redis", "Quartz", "Spring StateMachine", "OAuth2 SSO"],
      "topics": [
        {
          "id": "oa-hr",
          "title": "Attendance rules and performance",
          "background": "Landed flextime, overnight daily close, and multi-level performance in a standalone OA/HR system without reducing rules to punch-log sums.",
          "role": "Owned the attendance rule engine and performance state machine across shifts, offsets, daily close, and the review loop.",
          "flow": "Org/HR → shift rules → punch/leave/overtime offset → daily close → performance rollout → multi-level scoring archive",
          "engineeringBoundary": "Focuses on attendance and performance rules; master-data sync and SSO sit on the shared identity service.",
          "implementation": [
            "Attendance engine supports flextime and overnight shifts, splits attendance by calendar day, and recognizes holiday make-up days.",
            "Document-priority offsets combine punches with leave, overtime, and travel into traceable daily-close lines.",
            "Performance state machine: plan config, metric rollout, self/manager review, weighted scores, and version snapshots.",
            "Sharded daily-close batches by person so overnight shifts are neither double-counted nor dropped at day boundaries.",
            "Hire/transfer/leave drives account and role changes with an audit trail for attendance and performance reads.",
            "Shift templates stay decoupled from org calendars so holidays and flex windows can override by company/dept.",
            "Role-templated metrics support weighting, forced distribution, and read-only archives."
          ],
          "challenges": [
            "Overnight shifts, holiday make-up, and leave priority coexist; daily close must be recomputable and reconcilable.",
            "Rejected or edited metrics mid-flow need state-machine rollback while keeping historical score versions.",
            "Month-end close is compute-heavy; avoid long table locks and never score the same person twice concurrently."
          ],
          "outcome": "Attendance close and performance reviews are configurable, recomputable, and auditable.",
          "stack": ["Spring Boot", "MyBatis-Plus", "PostgreSQL", "Redis", "Spring StateMachine"]
        },
        {
          "id": "oa-sync",
          "title": "Master-data sync and SSO",
          "background": "Water users, orgs, and station permissions depend on OA master data. People changes must sync idempotently without duplicate accounts or delayed access.",
          "role": "Owned org master-data sync, OAuth2 SSO, and immediate water-side permission updates.",
          "flow": "Quartz paged pull → xtUserId dedupe → batch upsert → org permission mapping → SSO → water permissions",
          "engineeringBoundary": "Focuses on idempotent cross-system sync and SSO; water monitoring and the data agent belong to Smart Water Cloud.",
          "implementation": [
            "Quartz paged pulls of OA people, depts, roles, and stations, including in-batch duplicates.",
            "Dedupe in memory by xtUserId, then PostgreSQL batch upsert with resume-from-failure.",
            "Clean fields and employment status; push hire/transfer/leave into water project spaces and station permissions.",
            "OAuth2 SSO; water accounts and data permissions take effect as soon as org changes.",
            "Jobs record batch id, success/fail counts, and conflict samples for reconcile and replay.",
            "Failures split by type: PK conflict overwrites, illegal fields go to DLQ, source deletes become soft disable.",
            "Invalidate water permission cache by org node so old data rights do not linger after a transfer."
          ],
          "challenges": [
            "Duplicate pushes and partial failures coexist; write idempotently by business key and never let ON CONFLICT wreck the batch.",
            "Source data can change mid-page; use watermarks/update windows to catch up.",
            "Frequent org moves dirty permission cache; expire it and align with SSO sessions."
          ],
          "outcome": "The water platform identifies users and stations through one org permission model; sync is reconcilable and replayable by failure class.",
          "stack": ["Quartz", "PostgreSQL", "Redis", "OAuth2 SSO", "Paged sync", "Idempotent upsert"]
        }
      ]
    },
    {
      "id": "welink",
      "name": "Huawei WeLink",
      "company": "Adecco",
      "period": "Oct 2021 – Oct 2023",
      "pageSpan": 1,
      "summary": "Collaboration platform for large enterprises and universities. The open-platform team owned Xiaowei Search: unified retrieval across people, groups, and chats.",
      "businessContext": "WeLink Xiaowei Search needs one entry across people, groups, and chats, improved by profiles, personalization, recommendations, correction, and intent. Search consumes live changes and must also absorb history and missing-link backfill.",
      "painPoints": [
        "People, groups, and chats differ in shape, update rate, and retrieval strategy; Elasticsearch still needs one index contract.",
        "Live changes, historical backfill, and missing-link repair coexist; one ingest path cannot cover the full life cycle.",
        "Release work must cover data completeness, code quality, and process review."
      ],
      "buildGoals": [
        "Take live changes on Kafka/Flink and history/missing-link repair on Hadoop/Hive/Spark.",
        "Sync a unified contract into Elasticsearch for people, group, and chat search.",
        "Keep the path maintainable through design docs, QC/Review/Committer, and production delivery."
      ],
      "role": "Led cross-domain retrieval and affinity scoring, owned Flink realtime and Spark offline dual ingest, and served as QC/Committer for release reviews.",
      "flow": "Business changes & logs → Flink realtime cleanse → Spark offline reconcile → optimistic-lock write → correction & profile scoring → ES unified search",
      "engineeringBoundary": "Focuses on relevance, lake consistency, and engineering gates; field contracts and query SLAs are agreed with producers.",
      "outcome": "Delivered unified search and dual-path pipelines for 10M-scale enterprise users, where realtime increments and offline backfill coexist without overwriting each other.",
      "stack": ["Java", "Spring Boot", "Elasticsearch", "Kafka", "Flink", "Spark", "Hadoop", "Hive", "Redis", "Huawei Cloud"],
      "topics": [
        {
          "id": "welink-search",
          "title": "Unified retrieval and affinity scoring",
          "background": "Xiaowei Search serves 10M-scale enterprise users across people, groups, and chats, ranked by profile and affinity.",
          "role": "Owned people/group/chat search logic, user profiles, and the affinity scoring engine.",
          "flow": "Search intent → correction & pinyin → ES hybrid retrieval → profile/affinity boost → rank & highlight",
          "engineeringBoundary": "Focuses on the retrieval and ranking layer; field contracts are agreed with producers.",
          "implementation": [
            "Unified people/group/chat indexes with IK + pinyin + N-gram prefix hybrid retrieval.",
            "Function Score decay from org-tree distance and interaction frequency to improve first-screen precision.",
            "Completion Suggester for typeahead and spelling correction, including Chinese pinyin suggestions.",
            "search_after cursors instead of from/size deep paging, plus multi-level query cache on hot keys.",
            "Tuned bulk size, refresh_interval, and routing to control merges and GC under heavy writes.",
            "Split people/group/message indexes with aliases so rebuilds stay dark to the query entry.",
            "Highlight and snippet rules differ by field type so long chats do not drown person-name hits."
          ],
          "challenges": [
            "Long messages and short person names score differently; tune BM25 weights and decay to avoid bias.",
            "Heavy writes pressure merges and GC; control bulk, refresh, and replica strategy.",
            "Org changes drift affinity features; refresh profiles in batches and invalidate score cache."
          ],
          "outcome": "Shipped a unified search entry with correction, completion, and personalized ranking; index rebuilds do not break live queries.",
          "stack": ["Elasticsearch", "IK/pinyin", "Function Score", "Redis"]
        },
        {
          "id": "welink-data",
          "title": "Realtime / offline dual-path lake",
          "background": "Search needs live changes, historical backfill, and missing-link repair without clock races between writers.",
          "role": "Owned Kafka/Flink realtime pipelines and Hadoop/Hive/Spark offline compensation, plus the lake field contract.",
          "flow": "Binlog/logs → Kafka → Flink cleanse → version_type write to ES ← Spark reconcile backfill",
          "engineeringBoundary": "Guarantees life-cycle consistency across increment, stock, backfill, and missing-link repair.",
          "implementation": [
            "Kafka + Flink consume business changes and cleanse employee, group, and chat index fields near-realtime.",
            "Hadoop/Hive/Spark jobs handle history backfill, scheduled reconcile, and missing-link repair.",
            "ES version_type=external_gte so offline backfill cannot overwrite newer realtime increments.",
            "Flink ValueState / BroadcastState holds org dimensions and completes the unified contract before write.",
            "Dirty data goes to DLQ with alerts; field contracts, partitions, and SLAs are confirmed with producers.",
            "Reconcile jobs emit increment diffs to drive targeted backfill instead of full overwrites.",
            "Persist partitions and watermarks so failed jobs resume without duplicate backfill."
          ],
          "challenges": [
            "Dual writers on one document invert time unless external-version optimistic locks are used.",
            "Multi-source field contracts diverge; validate schema in the stream before search is polluted.",
            "Peak traffic requires stable Flink backpressure, checkpoint interval, and operator parallelism."
          ],
          "outcome": "Realtime and offline pipes cover the data life cycle; indexes stay reconcilable and resumable.",
          "stack": ["Kafka", "Flink", "Spark", "Hadoop", "Hive", "Elasticsearch"]
        }
      ]
    },
    {
      "id": "senge",
      "name": "Senge Smart Water Platform",
      "company": "Senge Automation Technology",
      "period": "Sep 2020 – Oct 2021",
      "pageSpan": 1,
      "summary": "Connected field devices, backend, and clients for plant monitoring, alarms, reports, remote control, and containerized release.",
      "businessContext": "The platform was in a 0-to-1 phase: ingest, state handling, realtime monitoring, and alarms extended into water-quality/pressure reports, archive, remote control, and production delivery. Features, realtime paths, and deploy had to land together.",
      "painPoints": [
        "Field data must travel from ingest and state cache to clients, and faults must enter an alarm operations loop.",
        "No reusable business skeleton existed; auth, monitoring, reports, archive, and audit had to be built together.",
        "Feature work ran in parallel with Docker deploy, release prep, and production incidents."
      ],
      "buildGoals": [
        "Deliver auth, device monitoring/alarms, water-quality/pressure reports, export, and archive.",
        "Build the field-to-dashboard path on Kafka, Redis, and WebSocket.",
        "Close a 0-to-1 loop across requirement, design, test, Docker deploy, and production support."
      ],
      "role": "Owned the 0-to-1 platform, spanning the realtime gateway, alarm debounce, auth/reports, and Docker production delivery.",
      "flow": "Field sensors → Kafka peak-shaving → Redis snapshots → WebSocket dashboards → remote-control checks → alarm debounce & dispatch",
      "engineeringBoundary": "Led 0-to-1 architecture across realtime channels, business foundation, database tuning, and env-consistent delivery.",
      "outcome": "Delivered a runnable smart-water platform from 0 to 1, connecting monitoring, alarms, reports/archive, and container operations.",
      "stack": ["Java", "Spring Boot", "Netty", "WebSocket", "Kafka", "Redis", "MySQL", "Docker"],
      "topics": [
        {
          "id": "senge-gateway",
          "title": "Realtime gateway and alarm control",
          "background": "Plant sensors report densely and need a high-concurrency long-lived channel plus alarm-storm control for jitter.",
          "role": "Led the Netty/WebSocket cluster, field data fan-out, and multi-channel alarm dispatch.",
          "flow": "Sensor time series → Kafka → WebSocket dashboards; faults → sliding-window debounce → SMS/WeChat/email",
          "engineeringBoundary": "Owns the realtime path from field ingest and long-lived push through alarm debounce and dispatch.",
          "implementation": [
            "Netty/WebSocket session management, heartbeats, and exponential reconnect with snapshot catch-up.",
            "Kafka decouples dense time series; Redis caches latest device state for high-frequency dashboards.",
            "Redis sliding windows aggregate jitter alarms and route SMS, WeChat mini program, and email by severity.",
            "Remote-control commands carry sequence, signature, and a distributed lock; timeout rolls back so pump/valve commands cannot replay.",
            "Client heartbeats and incremental catch-up restore dashboards after network jitter.",
            "Gateway cluster routes sessions in Redis so reconnects land on the node that holds the snapshot.",
            "Alarm templates version by device type and threshold so field threshold edits do not break the push channel."
          ],
          "challenges": [
            "Dense bursts become alarm storms; debounce, dedupe, and batch the push.",
            "Mass reconnect auth can saturate the gateway; use fast token checks and connection limits.",
            "Remote control of pumps/valves needs atomicity, anti-replay, and strict timeout rollback."
          ],
          "outcome": "Dashboards refresh with low delay, jitter alarms are suppressed, and remote-control commands roll back instead of replaying.",
          "stack": ["Netty", "WebSocket", "Kafka", "Redis", "WeChat mini program"]
        },
        {
          "id": "senge-platform",
          "title": "0-to-1 foundation and container delivery",
          "background": "Built auth, time-series reports, hot/cold archive, and standard Docker delivery so the platform can ship and operate.",
          "role": "Owned layered architecture and stack choices, and led RBAC, the time-series report engine, and Docker deploy.",
          "flow": "Requirements & stack → RBAC & audit → time-series reports & hot/cold archive → Docker Compose multi-env delivery",
          "engineeringBoundary": "Coordinates backend services, database tuning, config, and environment consistency.",
          "implementation": [
            "Spring Boot + MyBatis + MySQL RBAC, unified auth, and audit of sensitive actions.",
            "Daily/monthly/yearly aggregates and trends for quality, pressure, flow, and power, plus streaming Excel export.",
            "Time-based hot/cold archive: hot data stays queryable, cold data is compacted, primary stays light.",
            "Composite indexes and summary tables for multi-join and wide time aggregates.",
            "Dockerfile and Compose unify dev/test/prod config for one-command deploy.",
            "Audit covers login, remote control, export, and permission changes.",
            "Centralize datasource, Kafka, and alarm-channel config; containers inject by environment."
          ],
          "challenges": [
            "0-to-1 boundaries keep moving; keep layers clear and dependencies tight while still shipping.",
            "Realtime monitoring and historical reports must use separate channels so long SQL cannot block core IO.",
            "Scripts, config, and container deps drift across envs; parameterize startup for repeatable delivery."
          ],
          "outcome": "Left behind maintainable auth, reports, archive, audit, and one-command deploy.",
          "stack": ["Spring Boot", "MyBatis", "MySQL", "EasyExcel", "Docker Compose"]
        }
      ]
    }
  ]
}
```

中文 `oa` 的 `businessContext` 含「考勤」，英文对应句必须保留 attendance 语义。中文 WeLink `businessContext` 含「人员、群组和聊天记录」，英文用 `people, groups, and chats`。中文森格 `businessContext` 含「0-1 建设阶段」，英文用 `0-to-1 phase`。中文 AIoT topic 必须继续覆盖 UsrCloud/有人云、MQTT、Modbus、TCP/UDP、FanYi/繁易、Maituo/迈拓、Gukon/巨控 OPC/HTTP。

- [ ] **Step 5: 跑履历测试**

Run: `cd web && npm test -- --run src/data/resume-data.test.ts`

Expected: PASS。若中文断言失败，先核对改名是否丢内容，不要改中文事实。

- [ ] **Step 6: Commit**

```bash
git add web/src/data/resume-data.ts web/src/data/resume-data.test.ts web/src/data/resume-data.zh.json web/src/data/resume-data.en.json
git rm --ignore-unmatch web/src/data/resume-data.json
git commit -m "feat: split resume data into zh and en catalogs"
```

---

### Task 5: 模型说明与工程终端按 locale 导出

**Files:**
- Modify: `web/src/data/model-representations.ts`
- Modify: `web/src/data/engineering-console.ts`
- Modify: `web/src/data/engineering-console.test.ts`

**Interfaces:**
- Consumes: `Locale`
- Produces: `getModelRepresentation(id: string, locale?: Locale)`
- Produces: `getEngineeringTerminals(locale?: Locale)`
- Reducer 继续用 command id 校验；id 中英相同，校验可走 `getEngineeringTerminals('zh')`。

- [ ] **Step 1: 扩展工程终端测试**

现有中文断言改为 `JSON.stringify(getEngineeringTerminals('zh'))`。追加：

```ts
it('keeps English terminals on the same exhibit targets', () => {
  const en = getEngineeringTerminals('en');
  const exhibitIds = new Set(EXHIBITS.map((item) => item.id));
  const commands = en.flatMap((item) => item.commands);
  expect(commands.every((item) => exhibitIds.has(item.exhibitId))).toBe(true);
  expect(JSON.stringify(en)).toContain('10w+');
  expect(JSON.stringify(en)).toContain('AgentScope Java 2.0');
  expect(JSON.stringify(en)).toContain('UsrCloud');
});
```

- [ ] **Step 2: 跑测试确认英文断言失败**

Run: `cd web && npm test -- --run src/data/engineering-console.test.ts`

Expected: FAIL，`getEngineeringTerminals` 不存在或英文不含 UsrCloud。

- [ ] **Step 3: 实现 locale 分支**

`getModelRepresentation(id, locale = 'zh')`：把现有对象保留为中文目录 `MODEL_COPY.zh`；英文目录翻译每一个用户可见字符串。`id`、`order`、`shortLabel`、`subTitle`、`accentColor` 保持不变。不要只改 title。八个展项的英文 `entityName` / `components[].name` / `metaphor` / `keyMetrics` 都必须写出，来源是当前中文文件的逐条对译，例如：

```ts
'litree-overview': {
  ...sharedMeta.litreeOverview,
  title: 'Microservices & Data Foundation',
  entityName: 'Litree 10w+ station microservice hub and multi-tenant sharding base',
  entityDescription: 'Owns device identity, tenant isolation, business flow, and distributed-transaction coordination for 10w+ Litree stations.',
  visualMetaphorSummary: 'Central gimbal transaction core + four tenant compute pillars + surrounding shards',
  components: [
    { name: 'Floating dual-axis gimbal core', metaphor: 'Seata coordinator and Redis/Caffeine cache core' },
    { name: 'Four compute pillars', metaphor: '10w+ station gateway routing and ShardingSphere dynamic sharding' },
    { name: 'Surrounding data shards', metaphor: 'MyBatis-Plus tenant SQL intercept and hot/cold storage' },
    { name: 'Bidirectional energy pulses', metaphor: 'TransmittableThreadLocal tenant context and high-concurrency pipes' },
  ],
  keyMetrics: ['10w+ connected stations', '2000+ peak QPS', 'Zero cross-tenant reads'],
},
```

其余七项按同一字段集合完整翻译；禁止留中文句子在 `en` 目录。

英文 title 必须与 `ui.json` exhibits 一致：

- litree-overview: Microservices & Data Foundation
- litree-aiot: AIoT & Spatial Topology
- litree-agent: Water Data Agent
- oa-hr: Litree OA / HR
- welink-search: WeLink Unified Search
- welink-data: WeLink Dual-Path Data Lake
- senge-gateway: Senge Realtime Gateway
- senge-platform: Senge 0-to-1 Platform

`engineering-console.ts`：把现有数组作为 `zh`。英文数组只翻译 `label` 与 `lines[].value`；`id`、`command`、`exhibitId`、`title`、`shortTitle`、`index`、`accent`、`lines[].label` 保持不变。`有人云` → `UsrCloud`。导出：

```ts
export function getEngineeringTerminals(locale: Locale = 'zh'): readonly EngineeringTerminal[]
export const engineeringTerminals = getEngineeringTerminals('zh');
```

Reducer 里查找终端改为 `getEngineeringTerminals('zh').find(...)`（只用于校验 id）。

- [ ] **Step 4: 再跑测试**

Run: `cd web && npm test -- --run src/data/engineering-console.test.ts src/data/resume-data.test.ts`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/src/data/model-representations.ts web/src/data/engineering-console.ts web/src/data/engineering-console.test.ts
git commit -m "feat: localize model copy and engineering console"
```

---

### Task 6: 接入 HUD、面板、手机页与加载屏

**Files:**
- Modify: `web/src/App.tsx`
- Modify: `web/src/components/MuseumHud.tsx`
- Modify: `web/src/components/ExhibitPanel.tsx`
- Modify: `web/src/components/ResumeOverviewModal.tsx`
- Modify: `web/src/components/MobileResume.tsx`
- Modify: `web/src/components/MobileNotice.tsx`
- Modify: `web/src/components/CyberLoadingScreen.tsx`
- Modify: `web/src/components/EngineeringConsole.tsx`

**Interfaces:**
- Consumes: `useLocale`, `useTranslation`, `LanguageSwitcher`, `getResumeData`, `getTopic`, `getProject`, `getModelRepresentation`, `getEngineeringTerminals`
- `App` 读取 `locale` 并传给 `MuseumScene`（Task 7 才用该 prop；本 Task 先加上以免下一任务改 App 两次）。

- [ ] **Step 1: 给 `MuseumSceneProps` 增加 locale**

`web/src/scene/scene-types.ts`：

```ts
import type { Locale } from '../i18n/locale';

export interface MuseumSceneProps extends MotionProps, ExhibitSelectionProps {
  locale: Locale;
  panelOpen?: boolean;
  introActive: boolean;
  onIntroComplete: () => void;
  onReady: () => void;
  onFallback: () => void;
}
```

`App.tsx`：`const locale = useLocale();`，传给 `<MuseumScene locale={locale} ... />`。此时 `MuseumScene` 可接收但先不使用，避免 TypeScript 失败。

- [ ] **Step 2: 改 HUD**

- `useTranslation` + `getResumeData(useLocale())`
- 所有中文 chrome 改为 `t('hud.*')`
- PDF 链接改为 `t('resume.pdfHref')`
- 展位名改为 `t(\`exhibits.${exhibit.id}\`)`
- 在「核心履历」和 PDF 之间插入 `<LanguageSwitcher />`

- [ ] **Step 3: 改 ExhibitPanel / Overview / Mobile / Notice / Loading / Console**

规则：

- `const locale = useLocale(); const resumeData = getResumeData(locale);`
- `getTopic(exhibitId, locale)`、`getProject(id, locale)`、`getModelRepresentation(id, locale)`
- `EngineeringConsole` 内 `const terminals = getEngineeringTerminals(useLocale());` 替换 `engineeringTerminals`
- `CyberLoadingScreen` 的 `BOOT_LOGS` 放到组件内，用 `t('loading.logs.engine')` 等五条
- `MobileNotice`、面板折叠/关闭/分段标题全部走 `t()`
- 手机 Hero 操作区也放 `<LanguageSwitcher />`

- [ ] **Step 4: 删除 `resumeData` 兼容导出（若已无引用）**

搜 `from '../data/resume-data'` / `from './resume-data'`。场景文件若仍 import `resumeData`，留到 Task 7 再改。`resume-data.ts` 可暂时保留 `export const resumeData = getResumeData('zh')` 直到场景不再引用。

- [ ] **Step 5: 类型检查**

Run: `cd web && npm run check`

Expected: PASS，或仅剩场景文件尚未接 `locale` 的错误。若 `MuseumScene` 还未解构 `locale`，加 `_locale` 或 void 使用：`void props.locale;` 在 `SceneContent` 里，避免 unused 失败。

- [ ] **Step 6: Commit**

```bash
git add web/src/App.tsx web/src/scene/scene-types.ts web/src/components
git commit -m "feat: wire UI chrome and resume copy to i18n"
```

---

### Task 7: 3D 场景文案与纹理重绘

**Files:**
- Modify: `web/src/scene/scene-layout.ts`（删除 `label`）
- Modify: `web/src/scene/MuseumScene.tsx`
- Modify: `web/src/scene/environment/NeonWalls.tsx`
- Modify: `web/src/scene/textures/profile-hologram-texture.ts`
- Modify: `web/src/scene/ModelHologramTag.tsx`

**Interfaces:**
- `NeonWalls({ motionEnabled, locale })`
- `ModelHologramTags({ ..., locale })`
- `createCompactTagTexture(model, copy)` / `createInspectingTagTexture(model, copy)`，copy 含 ready/entityPrefix/click/inspecting/inspectHint
- `ProfileHologramContent` 增加 `coreValues` 与 `metrics: { label, value, desc, color }[]`

- [ ] **Step 1: 从 layout 删除 label**

`ExhibitLayout` 去掉 `label`。`EXHIBITS` 每项删除 `label` 字段。`scene-layout.test.ts` 不断言 label。HUD 已改用 `t('exhibits.*')`。

- [ ] **Step 2: 全息主屏与看板随 locale 重绘**

`NeonWalls` 接收 `locale`。用 `i18n.getFixedT(locale)` 和 `getResumeData(locale)`，**禁止** `useTranslation()`。

`billboardTextures` 的 `useMemo` 依赖 `[locale]`，副标题用 `t('scene.billboards.west1')` 等。cleanup 里 `dispose()` 旧纹理（已有模式，保持）。

主屏 canvas `useMemo` 依赖 `[locale, coverImage]`，传入：

```ts
createProfileHologramCanvas({
  name: resume.profile.name,
  title: resume.profile.title,
  experience: resume.profile.experience,
  coreValues: t('scene.hologram.coreValues'),
  metrics: [
    { label: 'GLOBAL WATER NETWORK', value: '100,000+', desc: t('scene.hologram.metricWater'), color: '#28d7e5' },
    { label: 'WELINK UNIFIED SEARCH', value: '10,000,000+', desc: t('scene.hologram.metricSearch'), color: '#ff6b3d' },
    { label: 'HIGH-FREQUENCY GATEWAY', value: '10,000+ TPS', desc: t('scene.hologram.metricGateway'), color: '#34d399' },
    { label: 'AGENT ORCHESTRATION', value: '99.95%', desc: t('scene.hologram.metricAgent'), color: '#c084fc' },
  ],
})
```

把原来写死在 `profile-hologram-texture.ts` 的中文 `CORE VALUES` 和四条 `desc` 改为使用 `content` 参数。纹理工厂不 import i18next。

- [ ] **Step 3: 展位标牌随 locale 重绘**

`SingleHologramTag` 增加 `locale`。`getModelRepresentation(exhibit.id, locale)`。`useMemo` 依赖 `[model, locale]`，创建新纹理前：

```ts
useMemo(() => {
  const texture = model ? createCompactTagTexture(model, copy) : null;
  return texture;
}, [model, locale]);
```

并用 `useEffect` dispose。硬编码中文改为 `i18n.getFixedT(locale)('scene.tag.ready')` 等。英文 `entityName` 可能更长，截断阈值从 28 提到 42，中英共用，避免英文标牌变成无意义省略。

`MuseumScene` 把 `props.locale` 传给 `NeonWalls` 和 `ModelHologramTags`。不要改 `sceneKey`。

- [ ] **Step 4: 跑场景边界测试与全局测试**

Run: `cd web && npm test -- --run && npm run check`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/src/scene web/src/data/resume-data.ts
git commit -m "feat: relocalize 3D billboards and hologram tags"
```

若本 Task 已无 `resumeData` 引用，删除该兼容导出。

---

### Task 8: PDF/Word 中英生成

**Files:**
- Modify: `tools/generate_resume.py`
- Create: `tools/test_resume_i18n.py`
- 运行脚本后会产生：
  - `Daopin-Fu-Senior-Java-Engineer.pdf`
  - `Daopin-Fu-Senior-Java-Engineer.docx`
  - 复制到 `web/public/resume/`

**Interfaces:**
- Produces: `OUTPUTS[lang] = { data, pdf, docx }`
- Produces: `CHROME[lang]` 章节标题字典
- CLI: `--lang zh|en|all`，默认 `all`

- [ ] **Step 1: 写路径解析失败测试**

在 `generate_resume.py` 顶部（`ROOT` 之后）加入可单测函数，测试文件只 import 这些符号。为避免测试加载字体失败，把路径函数放在字体注册之前，并用 `python -c` 或 unittest 只测纯函数。

`tools/test_resume_i18n.py`：

```python
import unittest
from pathlib import Path

import generate_resume as gr


class ResumeI18nTests(unittest.TestCase):
    def test_english_bundle_paths(self):
        bundle = gr.resume_bundle("en")
        self.assertTrue(str(bundle["data"]).endswith("resume-data.en.json"))
        self.assertIn("Daopin-Fu-Senior-Java-Engineer.pdf", str(bundle["pdf"]))
        self.assertIn("Daopin-Fu-Senior-Java-Engineer.docx", str(bundle["docx"]))

    def test_chinese_bundle_paths(self):
        bundle = gr.resume_bundle("zh")
        self.assertTrue(str(bundle["data"]).endswith("resume-data.zh.json"))
        self.assertIn("付道品-高级Java开发工程师.pdf", str(bundle["pdf"]))

    def test_chrome_has_matching_keys(self):
        self.assertEqual(set(gr.CHROME["zh"]), set(gr.CHROME["en"]))


if __name__ == "__main__":
    unittest.main()
```

因为脚本在 `tools/`，测试运行：

Run: `cd tools && python test_resume_i18n.py`

Expected: 先 FAIL（函数不存在）。把 `sys.path` 设为 `tools`，或在测试里：

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent))
import generate_resume as gr
```

- [ ] **Step 2: 实现 bundle 与 CHROME**

```python
OUTPUT_STEMS = {
    "zh": "付道品-高级Java开发工程师",
    "en": "Daopin-Fu-Senior-Java-Engineer",
}

def resume_bundle(lang: str) -> dict[str, Path]:
    stem = OUTPUT_STEMS[lang]
    return {
        "data": ROOT / "web" / "src" / "data" / f"resume-data.{lang}.json",
        "pdf": ROOT / f"{stem}.pdf",
        "docx": ROOT / f"{stem}.docx",
    }

CHROME = {
    "zh": {
        "page_before": "第 ",
        "page_after": f" / {TOTAL_PAGES:02d} 页",
        "stack": "技术栈  ",
        "museum": "3D 展馆",
        "overview": "核心简历",
        "overview_sub": "面向普通投递的能力概览与工作经历",
        "strengths": "核心能力",
        "experience": "工作经历",
        "position": "项目定位",
        "business": "业务场景",
        "pain": "现有痛点",
        "goals": "建设目标",
        "role": "本人角色",
        "flow": "业务链路",
        "boundary": "工程边界",
        "modules": "核心业务模块",
        "module": "模块",
        "outcome": "落地结果",
        "practice": "核心技术实践",
        "implementation": "核心实现",
        "challenges": "技术难点",
        "module_outcome": "模块结果",
        "bg": "背景",
        "role_short": "角色",
        "flow_short": "链路",
        "boundary_short": "边界",
        "result": "结果",
        "cover_domains": "分布式微服务 / 智慧水务 / AIoT & GIS / Agent 工程化",
        "cover_water": "国内外水站 / GLOBAL WATER STATIONS",
        "cover_statement": "> ENGINEERING STATEMENT // 职业定位与工程准则",
        "cover_statement_body": "★ 严谨工程边界意识 · 注重系统可恢复性、数据一致性与生产可观测性 · 具备端到端落地交付经验",
        "cover_base": "BASE: 深圳 / 广州 · 全职",
        "cover_confidential": "CONFIDENTIAL / {name} 个人技术经历与工程案例集",
        "overview_kicker": "高级 Java 开发工程师 · 3D 展馆: {website} · 微服务 / AIoT / GIS / Agent",
        "overview_position": "职业定位",
        "overview_strengths": "核心能力矩阵",
        "overview_experience": "工作经历",
        "overview_stack": "技术域索引",
        "system_arch": "系统架构",
        "system_position": "项目定位 / 系统边界",
        "system_background": "系统背景：业务场景 → 现有痛点 → 建设目标",
        "heading_flow": "总体业务链路",
        "heading_duty": "职责与工程边界",
        "system_practice": "系统实践",
        "impl_challenges": "核心实现与技术难点",
        "module_impl": "核心实现",
        "pdf_subject": "高级 Java 开发工程师简历",
        "item_join": "；",
        "item_stop": "。",
        "item_strip": "。；",
    },
    "en": {
        "page_before": "Page ",
        "page_after": f" / {TOTAL_PAGES:02d}",
        "stack": "Stack  ",
        "museum": "3D museum",
        "overview": "Core resume",
        "overview_sub": "Competency overview and work history for general applications",
        "strengths": "Core skills",
        "experience": "Work experience",
        "position": "Positioning",
        "business": "Business context",
        "pain": "Pain points",
        "goals": "Build goals",
        "role": "Role",
        "flow": "Flow",
        "boundary": "Engineering boundary",
        "modules": "Core modules",
        "module": "Module",
        "outcome": "Outcome",
        "practice": "Core technical practice",
        "implementation": "Implementation",
        "challenges": "Challenges",
        "module_outcome": "Module outcome",
        "bg": "Background",
        "role_short": "Role",
        "flow_short": "Flow",
        "boundary_short": "Boundary",
        "result": "Result",
        "cover_domains": "Distributed microservices / smart water / AIoT & GIS / Agent engineering",
        "cover_water": "Water stations worldwide / GLOBAL WATER STATIONS",
        "cover_statement": "> ENGINEERING STATEMENT // positioning and engineering bar",
        "cover_statement_body": "★ Strict ownership boundaries · recoverability, consistency, and production observability · end-to-end delivery",
        "cover_base": "BASE: Shenzhen / Guangzhou · full-time",
        "cover_confidential": "CONFIDENTIAL / {name} engineering casebook",
        "overview_kicker": "Senior Java Engineer · 3D museum: {website} · microservices / AIoT / GIS / Agent",
        "overview_position": "Positioning",
        "overview_strengths": "Core competency matrix",
        "overview_experience": "Work experience",
        "overview_stack": "Technology index",
        "system_arch": "System architecture",
        "system_position": "Positioning / system boundary",
        "system_background": "Background: context → pain points → goals",
        "heading_flow": "End-to-end flow",
        "heading_duty": "Role and engineering boundary",
        "system_practice": "System practice",
        "impl_challenges": "Implementation and challenges",
        "module_impl": "Implementation",
        "pdf_subject": "Senior Java Engineer resume",
        "item_join": "; ",
        "item_strip": ".;",
        "item_stop": ".",
    },
}
```

`join_items` 改为使用当前 `CHROME[lang]` 的 `item_join` / `item_strip` / `item_stop`。

删除模块级写死的 `DATA_PATH` / `DOCX_PATH` / `PDF_PATH`。`load_data(lang)` 读 `resume_bundle(lang)["data"]`。`build_docx(data, lang)` / `build_pdf(data, lang)` 把所有上表中文替换为 `CHROME[lang][key]`。封面/页脚里的姓名用 `data["profile"]["name"]`，不要写死付道品。`c.setTitle` / `setAuthor` / `setSubject` 用 profile + chrome。

页码函数：

```python
def add_page_number(paragraph, lang: str) -> None:
    chrome = CHROME[lang]
    run = paragraph.add_run(chrome["page_before"])
    ...
    end = paragraph.add_run(chrome["page_after"])
```

CLI：

```python
import argparse

def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--lang", choices=("zh", "en", "all"), default="all")
    args = parser.parse_args()
    langs = ("zh", "en") if args.lang == "all" else (args.lang,)
    public_resume_dir = ROOT / "web" / "public" / "resume"
    public_resume_dir.mkdir(parents=True, exist_ok=True)
    for lang in langs:
        data = load_data(lang)
        bundle = resume_bundle(lang)
        build_docx(data, lang, bundle["docx"])
        build_pdf(data, lang, bundle["pdf"])
        shutil.copy2(bundle["pdf"], public_resume_dir / bundle["pdf"].name)
        shutil.copy2(bundle["docx"], public_resume_dir / bundle["docx"].name)
        print(bundle["docx"])
        print(bundle["pdf"])
```

把 `lang` 向下传到所有 `draw_*` / `add_docx_*`。不要改 7 页结构、配色、自适应字号。字体仍是 Microsoft YaHei。

- [ ] **Step 3: 跑 Python 单测**

Run: `cd tools && python test_resume_i18n.py`

Expected: PASS。

- [ ] **Step 4: 生成英文稿**

Run: `python tools/generate_resume.py --lang en`

Expected: 根目录与 `web/public/resume/` 出现 `Daopin-Fu-Senior-Java-Engineer.pdf/.docx`。打开 PDF 首页应看到 Daopin Fu，且可搜索 `Senior Java Engineer`。中文稿用 `--lang zh` 或默认 `all` 回归，确认中文 PDF 仍是付道品。

若英文溢出，只压缩英文 JSON 句子，不改版式。

- [ ] **Step 5: Commit**

```bash
git add tools/generate_resume.py tools/test_resume_i18n.py
git add "Daopin-Fu-Senior-Java-Engineer.pdf" "Daopin-Fu-Senior-Java-Engineer.docx"
git add "web/public/resume/Daopin-Fu-Senior-Java-Engineer.pdf" "web/public/resume/Daopin-Fu-Senior-Java-Engineer.docx"
git add "付道品-高级Java开发工程师.pdf" "付道品-高级Java开发工程师.docx" "web/public/resume/"
git commit -m "feat: generate English resume PDF and Word"
```

若二进制 PDF 过大或不便入库，至少提交 `web/public/resume/Daopin-Fu-Senior-Java-Engineer.pdf`，因为站点下载依赖它。中文 PDF 若此次未变化可不重复提交。

---

### Task 9: 首屏防闪与 README

**Files:**
- Modify: `web/index.html`
- Modify: `README.md`

- [ ] **Step 1: 内联启动脚本**

在 `index.html` 的 `<head>` 末尾、现有 style 之后加入：

```html
<script>
  (function () {
    var params = new URLSearchParams(location.search);
    var raw = params.get('lang') || (function () {
      try { return localStorage.getItem('i18nextLng'); } catch (e) { return null; }
    })();
    var base = raw ? String(raw).toLowerCase().split('-')[0] : 'zh';
    if (base !== 'en') return;
    document.documentElement.lang = 'en';
    document.title = 'Daopin Fu | Senior Java Engineer';
    var desc = document.querySelector('meta[name="description"]');
    if (desc) desc.setAttribute('content', 'Daopin Fu, Senior Java Engineer. Smart water, Java microservices, AIoT/GIS, and Agent engineering.');
    var title = document.querySelector('.init-title');
    if (title) title.textContent = 'Initializing museum assets...';
  })();
</script>
```

脚本必须与 `resolveLocale` 同序：URL `lang` 优先于 `i18nextLng`。不要读 `navigator.language`。

- [ ] **Step 2: 更新 README**

把「简历单一数据源」一行改为：

| `web/src/data/resume-data.zh.json` | 中文履历事实源 |
| `web/src/data/resume-data.en.json` | 英文履历事实源 |

生成命令改为：

```powershell
python tools/generate_resume.py
```

默认生成中英两套。需要单语时用 `--lang zh` 或 `--lang en`。英文 PDF：`/resume/Daopin-Fu-Senior-Java-Engineer.pdf`。不要把 README 全文译成英文。

- [ ] **Step 3: 最终验证**

Run:

```powershell
cd web
npm test -- --run
npm run check
```

Expected: 全部 PASS。

手动：`npm run dev`，确认 HUD 能切中/英、`?lang=en` 直达英文、刷新保持、手机降级页也能切、3D 标牌换文案且 Canvas 不重载、英文模式 PDF 链接指向英文文件。

- [ ] **Step 4: Commit**

```bash
git add web/index.html README.md
git commit -m "docs: document bilingual resume sources and boot locale"
```

---

## Self-review

1. Spec coverage: locale 解析、i18next、平行 JSON、开关位置、3D 重绘、PDF `--lang`、测试、不做浏览器自动跳转 —— 均有对应 Task。
2. Placeholders: 无 TBD；英文履历全文写在 Task 4；UI 词典全文写在 Task 2。
3. Types: `Locale`、`getResumeData(locale)`、`MuseumSceneProps.locale`、`changeAppLanguage` 前后一致。
